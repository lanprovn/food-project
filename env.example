# Environment Configuration
# Copy this to .env.local if needed

# App Configuration
VITE_APP_NAME=Ocha Việt POS
VITE_APP_VERSION=1.0.0
VITE_APP_ENV=development

# Note: This project uses mock data stored in localStorage
# No backend API configuration is required
